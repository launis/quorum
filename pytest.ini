[pytest]
testpaths = tests
python_files = test_*.py
norecursedirs = scripts data .git __pycache__ tests/output
addopts = -v
pythonpath = .
markers =
    live: mark test as running against real external services (slow, requires env vars).
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
