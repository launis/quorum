# Phase C5: Update ExecutionController and ReportController

> Source: Red-Teamed Implementation Plan Phase C5
> Status: PLACEHOLDER — Detailed plan to be generated by re-invoking Tier 1 Planner after C0-C4 are committed.

## Summary

- **`execution_controller.dart`:** Fix `_performHeavyFetch` to use `ReportDataDto.fromJson()` instead of `parseInBackground(jsonEncode(...))`.
- **`report_controller.dart`:** Replace `fromBackendResponse(rawData)` with `ReportDataDto.fromJson(rawData)`.

## Files Affected (Preview)

| Role | File | Action |
|---|---|---|
| TARGET | `client_app_v2/lib/features/execution/controllers/execution_controller.dart` | Fix heavy fetch path |
| TARGET | `client_app_v2/lib/features/execution/controllers/report_controller.dart` | Use fromJson |
