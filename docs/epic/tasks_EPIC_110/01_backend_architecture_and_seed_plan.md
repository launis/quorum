# Phase 1 & 2: Backend Architecture & Seed Data Migration (Epic 110)

## Objective
Implement Phase 1 (Jinja PDF Duct-Tape Removal) and Phase 2 (Terminology Sovereignty) for the backend domain as per `@[c:\src\quorum\docs\epic\EPIC_110_dumb_painter_sdui_architecture.md]`.

## Architectural Invariants
- **Rule Injection**: Must adhere to `@[c:\src\quorum\.agents\rules\00-antigravity-core.md]`, `@[c:\src\quorum\.agents\rules\01-python-backend.md]`, and `@[c:\src\quorum\.agents\rules\03_seed_vault.md]`.
- **Dumb Painter Law**: No text, title, or structural formatting shall be generated, hardcoded, or deduced by the PDF template. All printed text MUST come exclusively from the backend payload (`ReportDataDTO`).
- **Pydantic Strictness**: `ConfigDict(strict=True, extra='forbid')` remains enforced.

## Targets

### 1. `@[c:\src\quorum\backend_v2\models\v2_core.py]` (TARGET)
- Add `matrix_column_labels: dict[str, I18nText]` to `OutputLayoutBlock`. Default factory empty dict.
- Add `extension_labels: dict[LaxXaiExtensionType, I18nText]` to `OutputLayoutBlock`. Default factory empty dict.

### 2. `@[c:\src\quorum\backend_v2\models\dtos\output_profile.py]` (TARGET)
- Ensure that any response mapping aligns with the `OutputLayoutBlock` changes.
- (Verify structure if needed)

### 3. `@[c:\src\quorum\backend_v2\seed\seed_data.json]` (TARGET)
- Migrate existing layout blocks in the output profiles to include `matrix_column_labels` and `extension_labels` where applicable, or at least initialize them as empty objects to satisfy strict Pydantic parsing if they aren't generated properly.

### 4. `@[c:\src\quorum\backend_v2\templates\report_template.jinja2]` (TARGET)
- Remove hardcoded UI intelligence: e.g. `<h2 style="...">Yhteenveto</h2>` -> use `block.resolved_title` (this property needs to be mapped in `blueprint.py` later, so assume `block.title` or whatever is passed in `ReportDataDTO`).
- Remove static metadata labels that were hardcoded.
- Ensure the `render_sdui_blocks` macro or the main loops render the `resolved_title` instead of hardcoded Finnish strings like "Kokonaiskeskiarvo" or "Käyttäjä", mapping to `l10n` objects generated by the backend payload.

## Testing & Quality Gate Plan
- Run Backend Audit: `uv run python scripts/backend_audit_loop.py backend_v2/models/ --test`
- Run Seed Audit: `uv run python backend_v2/seed/run_seed.py local`
