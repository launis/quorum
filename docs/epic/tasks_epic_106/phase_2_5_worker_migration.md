# Phase 2.5: Worker Rendering Pipeline Migration & Atomic `formatting_directives` Removal

> **Source**: Epic 106 — Phase 2.5
> **Domain**: Backend (Python)
> **Status**: PLACEHOLDER — Detailed plan to be generated by Tier 1 Planner after Phase 2 completion.

## Scope Summary

- Refactor `worker.py` lines 770-798 to source synthesis instructions from DAG-produced data (PromptBlock + OutputLayoutBlock.synthesis).
- Remove `formatting_directives` from `worker.py` atomically with `llm.py` (Phase 2 Milestone 2.4).
- Source `allowed_exports` from `Workflow`-level configuration (new field from Phase 0/1).
- Enforce Fail-Fast: No `.get()` or `hasattr()` fallbacks for removed fields.

## Target Files (Estimated)

1. `backend_v2/worker.py` (lines 770-798) — PRIMARY TARGET
2. `backend_v2/services/orchestrator/synthesis_distiller.py` — May need updates for historical_context_mode sourcing
3. Related test files: `test_worker_synthesis.py`, `test_worker.py`, `test_worker_synthesis_hydration.py`
