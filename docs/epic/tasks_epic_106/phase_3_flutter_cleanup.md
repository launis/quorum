# Phase 3: Flutter Admin UI Cleanup & Freezed Synchronization

> **Source**: Epic 106 — Phase 3
> **Domain**: Frontend (Flutter / Dart)
> **Status**: PLACEHOLDER — Detailed plan to be generated by Tier 1 Planner after Phase 2.5 completion.

## Scope Summary

- Update `OutputProfile` and `EmbeddedOutputProfile` Freezed classes in `client_app_v2/lib/features/studio/models/output_profile.dart` — remove `synthesis`, `formattingDirectives`, `matrixColumnLabels` fields.
- Run `dart run build_runner build -d` to regenerate `.freezed.dart` and `.g.dart` files.
- Audit `SynthesisConfigDTO` Freezed model in `client_app_v2/lib/features/execution/models/synthesis_config_dto.dart` — if only consumed via OutputProfile, DELETE it. If consumed via `OutputLayoutBlock.synthesis`, update imports.
- Scrub frontend Repository classes of deprecated field payloads.
- Update Flutter Admin Dashboard UI to reflect the pruned model (clean single-column settings card).
- Verify `test_enum_parity.py` passes after all changes.

## Target Files (Estimated)

1. `client_app_v2/lib/features/studio/models/output_profile.dart` — PRIMARY TARGET
2. `client_app_v2/lib/features/execution/models/synthesis_config_dto.dart` — SCOPE AUDIT
3. `client_app_v2/lib/features/studio/views/` — UI cleanup
4. `client_app_v2/lib/features/studio/providers/` — Repository payload scrubbing
