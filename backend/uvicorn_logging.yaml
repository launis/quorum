# YAML config for uvicorn to match our logging format
version: 1
disable_existing_loggers: false
formatters:
  default:
    format: "%(asctime)s | %(levelname)s | [SYSTEM] | %(name)s | %(message)s"
    datefmt: "%Y-%m-%d %H:%M:%S"
  access:
    format: "%(asctime)s | %(levelname)s | [SYSTEM] | uvicorn.access | %(client_addr)s - \"%(request_line)s\" %(status_code)s"
    datefmt: "%Y-%m-%d %H:%M:%S"
handlers:
  default:
    class: logging.StreamHandler
    formatter: default
    stream: ext://sys.stdout
  access:
    class: logging.StreamHandler
    formatter: access
    stream: ext://sys.stdout
loggers:
  uvicorn:
    level: INFO
    handlers:
      - default
    propagate: false
  uvicorn.error:
    level: INFO
    handlers:
      - default
    propagate: false
  uvicorn.access:
    level: INFO
    handlers:
      - access
    propagate: false
